__pycache__/
*.py[cod]
.venv/
venv/
env/
.env
# Bake the embeddings cache INTO the image so Cloud Run startup is fast.
# (Comment the next line out if you want to force a re-embed at startup.)
# data/embeddings_cache/
# Keep the CSV — it gets baked into the image
# Keep data/*.json and data/*.jsonl if present (aliases/feedback snapshots)
tests/
*.egg-info/
