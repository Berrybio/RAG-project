# ============================================================================
# LLM provider — switch between vendors without touching code
# ============================================================================
# Supported values: anthropic | openai | deepseek | kimi
LLM_PROVIDER=anthropic
# Model identifier passed to the underlying API. Examples:
#   anthropic: claude-sonnet-4-20250514, claude-opus-4-...
#   openai:    gpt-4o, gpt-4o-mini, o1-...
#   deepseek:  deepseek-chat, deepseek-reasoner
#   kimi:      moonshot-v1-8k, moonshot-v1-32k, moonshot-v1-128k
# Falls back to CLAUDE_MODEL below when unset (backward compatibility).
LLM_MODEL=claude-sonnet-4-20250514

# ----- API keys: only the active provider's key is required -----
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=
DEEPSEEK_API_KEY=
MOONSHOT_API_KEY=          # for the kimi provider

# Optional: override Moonshot's regional endpoint (default: api.moonshot.ai)
# MOONSHOT_BASE_URL=https://api.moonshot.cn/v1

# ============================================================================
# Retrieval
# ============================================================================
VOYAGE_API_KEY=                        # omit to fall back to TF-IDF
RETRIEVER_TYPE=voyage                  # voyage | tfidf
CSV_PATH=/app/data/Breast_Cancer-RECRUITING-phase2-625.csv

# ============================================================================
# Misc
# ============================================================================
LOG_LEVEL=info
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:80

# Backward-compat: still read by the factory if LLM_MODEL is unset.
CLAUDE_MODEL=claude-sonnet-4-20250514
